name: Günlük İlan Scraper

on:
  schedule:
    # Her gün saat 09:00 UTC (12:00 TR) çalışır
    - cron: '0 9 * * *'
  
  workflow_dispatch: # Manuel tetikleme için

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        persist-credentials: true
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests beautifulsoup4 lxml
    
    - name: Run scraper
      run: |
        python github_scraper.py
    
    - name: Commit and push results
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add data/*.json
        git diff --quiet && git diff --staged --quiet || git commit -m "🤖 Günlük ilan taraması - $(date +'%Y-%m-%d %H:%M')"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Send Telegram notification
      if: success()
      run: |
        # JSON dosyasından bilgileri çek
        TOPLAM=$(jq -r '.toplam' data/latest.json)
        KEDI=$(jq -r '.kedi | length' data/latest.json)
        KOPEK=$(jq -r '.kopek | length' data/latest.json)
        TARIH=$(date +'%Y-%m-%d %H:%M')
        
        # Mesaj hazırla
        MESSAGE="🤖 *Gören Duyan Scraper*%0A%0A"
        MESSAGE+="✅ Tarama tamamlandı!%0A%0A"
        MESSAGE+="📊 *Sonuçlar:*%0A"
        MESSAGE+="• Toplam: *${TOPLAM}* ilan%0A"
        MESSAGE+="• 🐱 Kedi: *${KEDI}*%0A"
        MESSAGE+="• 🐕 Köpek: *${KOPEK}*%0A%0A"
        MESSAGE+="🕐 Tarih: ${TARIH}%0A%0A"
        MESSAGE+="🔗 [Sonuçları Görüntüle](https://github.com/${{ github.repository }}/tree/main/data)"
        
        # Telegram'a gönder
        curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="${MESSAGE}" \
          -d parse_mode="Markdown" \
          -d disable_web_page_preview=true

